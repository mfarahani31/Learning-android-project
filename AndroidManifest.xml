<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.mainproject"
          android:versionCode="1"
          android:versionName="1.0">
    <uses-sdk android:minSdkVersion="16"/>
    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher">
        <activity android:name=".FirstActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".pages.About" android:theme="@android:style/Theme.Dialog"/>
        <activity android:name=".MyActivity"  android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".pages.News"/>
        <activity android:name=".pages.Search" android:windowSoftInputMode="stateVisible"/>

    </application>
</manifest>
